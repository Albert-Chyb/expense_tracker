import { ProgressSpinnerFixedDirective } from './progress-spinner-fixed.directive';

describe('ProgressSpinnerFixedDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressSpinnerFixedDirective();
    expect(directive).toBeTruthy();
  });
});
