import { StyledInputSuffixDirective } from './styled-input-suffix.directive';

describe('StyledInputSuffixDirective', () => {
  it('should create an instance', () => {
    const directive = new StyledInputSuffixDirective();
    expect(directive).toBeTruthy();
  });
});
