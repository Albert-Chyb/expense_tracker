import { FilesInputErrorsDirective } from './files-input-errors.directive';

describe('FilesInputErrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new FilesInputErrorsDirective();
    expect(directive).toBeTruthy();
  });
});
